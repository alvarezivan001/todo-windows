// import {proje}


const addProject = (project) => {
  // called by viewer, creates to model
};

const deleteProject = (project) => {
  // called by viewer, creates to model
};

const loadToDoOnClick = (project) => {
  // when project is clicked, will load to dos into body
  // so calls to do viewer
}
